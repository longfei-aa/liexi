# UI Module

Owns C++ UI view models and widget-facing data structures.

Milestone 1 can use a Blueprint-only HUD. Add C++ here when HUD state needs shared formatting or replication-safe view models.
